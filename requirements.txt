# Python dependencies for the loaders and the analysis stages.
# Install into a venv in the clone:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# (macOS system python refuses a plain `pip install` under PEP 668, hence the venv.)
pymongo>=4.6                  # loaders/set_copilot_entities.py; also provides bson
numpy>=1.24                   # analysis/ statistics

# loaders/media_hash.py - the creative hash must match the analysis service's, so these are the
# same libraries it hashes with. Swapping them changes the hash and breaks the warehouse join.
ImageHash>=4.3                # phash (images) and average_hash (video frames)
opencv-python-headless>=4.9   # frame sampling; headless build - no GUI libs needed
pillow>=10                    # image decoding for ImageHash
requests>=2.31                # media download
